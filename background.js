// background.js - Placeholder for future background tasks
// Currently handles the keyboard shortcut (Ctrl+Shift+N) automatically through manifest commands.
chrome.runtime.onInstalled.addListener(() => {
    console.log("Notesy Extension Installed Successfully");
});
